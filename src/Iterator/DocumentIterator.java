package Iterator;




/* public class DocumentIterator implements Iterator<BredText> {
    private BredText[] sections;
    private int position = 0;

    public DocumentIterator(BredText[] sections) {
        this.sections = sections;
    }

    @Override
    public boolean hasNext() {
        return position < sections.length;
    }

    @Override
    public BredText next() {
        if (this.hasNext()) {
            return sections[position++];
        } else {
            return null;
        }
    }
} */
